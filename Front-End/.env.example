# Copy this file to .env (or .env.local) and adjust as needed.
# Your ASP.NET Core API's base URL, including the https port and /api segment.
VITE_API_BASE_URL=https://localhost:7000/api

# Stripe publishable key (safe to expose client-side, starts with pk_).
# Get this from your Stripe dashboard - it must match the secret key your backend uses.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
